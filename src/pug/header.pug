extends helpers/layout.pug
block append head
  style.
    .t-content {
      background: var(--grey);
      height: 200vh;
    }
block content
  .page

    include components/header/index.pug

    main
      .container
        .t-content
